/**
 * Domain layer: entities, invariants, and the ports they own.
 *
 * <p>Layer rule: plain Java only. MUST NOT import a web framework, an ORM, or any transport type. Enforced by LayerDependencyTest.
 */
package dev.quicknote.notes.domain;
