/**
 * Application layer: use cases, transaction boundaries, and authorization.
 *
 * <p>Layer rule: may depend on domain only. Owns ownership checks; MUST NOT expose transport or persistence types.
 */
package dev.quicknote.notes.application;
