/**
 * Credential verification and identity extraction.
 *
 * <p>Layer rule: identity comes from the verified token only. This service never issues, refreshes, or revokes credentials.
 */
package dev.quicknote.security;
